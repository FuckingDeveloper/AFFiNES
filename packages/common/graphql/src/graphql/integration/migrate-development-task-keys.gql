mutation migrateDevelopmentTaskKeys(
  $workspaceId: String!
  $fromPrefix: String!
  $toPrefix: String!
) {
  migrateDevelopmentTaskKeys(
    workspaceId: $workspaceId
    fromPrefix: $fromPrefix
    toPrefix: $toPrefix
  ) {
    migrated
    skipped
  }
}
