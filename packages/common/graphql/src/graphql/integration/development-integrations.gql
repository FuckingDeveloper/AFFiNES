query developmentIntegrations($workspaceId: String!) {
  workspace(id: $workspaceId) {
    developmentIntegrations {
      id
      workspaceId
      provider
      name
      baseUrl
      username
      enabled
      hasToken
      hasWebhookSecret
      webhookUrl
      createdAt
      updatedAt
      repositories {
        id
        externalId
        name
        fullName
        webUrl
        defaultBranch
        enabled
      }
    }
  }
}
